<style>
    .emp-range-widget {
        width: 100%;
    }

    .erw-row1, .erw-row2 {
        display: flex;
        align-items: center;
        justify-content: space-between;
        margin-bottom: 8px;
        gap: 12px;
    }

    /* buttons row */
    .erw-btns {
        display: flex;
        gap: 8px;
    }

    .erw-btn {
        min-width: 110px;
        height: 36px;
        padding: 6px 12px;
        border-radius: 6px;
        cursor: pointer;
        border: 1px solid #d0b4b4;
        background: #ffeef3;
        font-size: 13px;
    }

        .erw-btn.yellow {
            background: #fff7d6;
            border-color: #e6d6a8;
        }

    /* input */
    .erw-employee {
        flex: 1;
        max-width: 450px;
    }

        .erw-employee input {
            width: 100%;
            height: 36px;
            border: 1px solid #dcdcdc;
            border-radius: 6px;
            padding: 6px 10px;
        }

    /* row 2 */
    .erw-row2 {
        justify-content: space-between;
    }

    .erw-right-group {
        display: flex;
        align-items: center;
        gap: 12px;
    }

    .erw-title {
        color: #2d9a5a;
        font-size: 13px;
        white-space: nowrap; /* important! */
    }

    /* date fields */
    .erw-date-group {
        display: flex;
        align-items: center;
        gap: 10px;
    }

        .erw-date-group label {
            font-size: 13px;
            color: #2d9a5a;
            margin-right: 4px;
            white-space: nowrap;
        }

        .erw-date-group input[type="date"] {
            width: 135px;
            height: 34px;
            padding: 4px 8px;
            border-radius: 6px;
            border: 1px solid #d0d0d0;
        }

    /* actions */
    .erw-actions {
        display: flex;
        gap: 8px;
    }

    .erw-action {
        height: 34px;
        padding: 6px 12px;
        border-radius: 6px;
        border: 1px solid #bfbfbf;
        background: #fff;
        cursor: pointer;
        font-size: 13px;
    }

    .erw-small-date {
        color: #777;
        font-size: 12px;
        margin-left: 8px;
        white-space: nowrap;
    }
</style>
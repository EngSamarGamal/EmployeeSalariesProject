<style>
    .emp-head {
        background: #efefef;
        border: 1px solid #ccc;
        padding: 12px 20px;
        display: flex;
        align-items: center;
        justify-content: center; /* force perfect centering */
        width: 100%;
        box-sizing: border-box;
        font-family: Tahoma, Arial;
        position: relative; /* allow fake left/right blocks */
    }

        /* fake equal-width blocks right & left */
        .emp-head::before,
        .emp-head::after {
            content: "";
            width: 200px; /* same width both sides */
            height: 1px; /* invisible but keeps balance */
            display: block;
        }

        .emp-head .center-text {
            text-align: center;
            color: #c33;
            line-height: 1.4;
        }

            .emp-head .center-text .title {
                font-weight: 700;
                font-size: 20px;
                color: #c33;
            }

            .emp-head .center-text .subtitle {
                font-size: 15px;
                margin-top: 4px;
                color: #c33;
            }
    /* ===== Pagination Style (RTL + Bootstrap-like) ===== */

    .dataTables_wrapper .dataTables_paginate {
        float: left;
        margin-top: 1rem;
    }

        .dataTables_wrapper .dataTables_paginate .pagination {
            display: inline-flex;
            gap: 6px;
            padding-left: 0;
            list-style: none;
            margin: 0;
        }

        .dataTables_wrapper .dataTables_paginate .paginate_button {
            min-width: 36px;
            height: 36px;
            line-height: 34px;
            text-align: center;
            border-radius: 8px;
            border: 1px solid rgba(0,0,0,0.15);
            background: #fff;
            padding: 0 8px;
            cursor: pointer;
        }

            .dataTables_wrapper .dataTables_paginate .paginate_button.current {
                background: #5b6bff;
                color: #fff !important;
                border-color: rgba(91,107,255,0.9);
            }

            .dataTables_wrapper .dataTables_paginate .paginate_button.disabled {
                opacity: 0.45;
                cursor: not-allowed;
            }

    .dataTables_wrapper .dataTables_info {
        float: right;
        margin-top: 1rem;
    }
</style>
